import React from "react"
import * as S from "./CartContainer.styles"

const CartContainer = ({ children }) => {
    return <S.CartContainer>{children}</S.CartContainer>
}

export default CartContainer
